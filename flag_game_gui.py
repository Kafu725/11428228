import tkinter as tk
from tkinter import messagebox, simpledialog
import random
import os
import sys
try:
    from PIL import Image, ImageTk
except ImportError:
    Image = None
    ImageTk = None

# ==========================================
# 國旗與題庫資料 (格式: 國家, 國旗, 首都, [提示列表])
# ==========================================
COUNTRY_DATA = [
    ("台灣", "🇹🇼", "台北", ["珍珠奶茶的發源地", "擁有曾經的世界第一高樓 101", "位於東亞的島嶼"]),
    ("阿富汗", "🇦🇫", "喀布爾", ["位於中亞", "盛產石榴與地毯", "多山地形"]),
    ("阿爾巴尼亞", "🇦🇱", "地拉那", ["雙頭鷹國旗", "巴爾幹半島", "碉堡王國"]),
    ("阿爾及利亞", "🇩🇿", "阿爾及爾", ["北非最大國家", "撒哈拉沙漠", "盛產石油"]),
    ("安道爾", "🇦🇩", "安道爾城", ["位於法國與西班牙之間", "庇里牛斯山小國", "滑雪勝地"]),
    ("安哥拉", "🇦🇴", "羅安達", ["非洲西南部", "曾是葡萄牙殖民地", "盛產石油與鑽石"]),
    ("安地卡及巴布達", "🇦🇬", "聖約翰", ["加勒比海島國", "365個海灘", "板球運動"]),
    ("阿根廷", "🇦🇷", "布宜諾斯艾利斯", ["探戈舞", "足球巨星梅西", "南美洲國家"]),
    ("亞美尼亞", "🇦🇲", "葉里溫", ["高加索地區", "世界上第一個基督教國家", "亞拉拉特山"]),
    ("澳洲", "🇦🇺", "坎培拉", ["有袋鼠和無尾熊", "雪梨歌劇院", "位於南半球"]),
    ("奧地利", "🇦🇹", "維也納", ["音樂之都", "莫札特", "阿爾卑斯山滑雪"]),
    ("亞塞拜然", "🇦🇿", "巴庫", ["火之國", "裏海石油", "高加索山脈"]),
    ("巴哈馬", "🇧🇸", "拿索", ["加勒比海", "豬島", "清澈海水"]),
    ("巴林", "🇧🇭", "麥納瑪", ["波斯灣島國", "F1賽車", "珍珠採集歷史"]),
    ("孟加拉", "🇧🇩", "達卡", ["人口密度極高", "恆河三角洲", "成衣出口大國"]),
    ("巴貝多", "🇧🇧", "布里奇敦", ["蕾哈娜的故鄉", "加勒比海", "飛魚料理"]),
    ("白俄羅斯", "🇧🇾", "明斯克", ["東歐內陸國", "歐洲野牛", "馬鈴薯料理"]),
    ("比利時", "🇧🇪", "布魯塞爾", ["巧克力與鬆餅", "歐盟總部所在地", "尿尿小童"]),
    ("貝里斯", "🇧🇿", "貝爾墨潘", ["中美洲", "大藍洞", "馬雅文明"]),
    ("貝南", "🇧🇯", "波多諾伏", ["巫毒教發源地", "西非國家", "奴隸海岸歷史"]),
    ("不丹", "🇧🇹", "廷布", ["雷龍之國", "幸福指數(GNH)", "喜馬拉雅山"]),
    ("玻利維亞", "🇧🇴", "蘇克雷", ["天空之鏡(烏尤尼鹽沼)", "南美內陸國", "高原地形"]),
    ("波士尼亞與赫塞哥維納", "🇧🇦", "塞拉耶佛", ["巴爾幹半島", "第一次世界大戰導火線", "東西文化交匯"]),
    ("波札那", "🇧🇼", "嘉柏隆里", ["非洲大象", "卡拉哈里沙漠", "鑽石開採"]),
    ("巴西", "🇧🇷", "巴西利亞", ["亞馬遜雨林", "森巴舞與足球王國", "里約熱內盧基督像"]),
    ("汶萊", "🇧🇳", "斯里巴加灣市", ["婆羅洲石油富國", "水上村落", "伊斯蘭君主制"]),
    ("保加利亞", "🇧🇬", "索菲亞", ["玫瑰之國", "優格", "巴爾幹山脈"]),
    ("布吉納法索", "🇧🇫", "瓦加杜古", ["西非內陸", "正人君子之國", "非洲電影節"]),
    ("蒲隆地", "🇧🇮", "基特加", ["非洲之心", "坦干依喀湖", "鼓樂文化"]),
    ("柬埔寨", "🇰🇭", "金邊", ["吳哥窟", "湄公河", "高棉微笑"]),
    ("喀麥隆", "🇨🇲", "雅溫德", ["非洲小縮影", "足球強國", "可可生產"]),
    ("加拿大", "🇨🇦", "渥太華", ["國旗上有楓葉", "世界上國土面積第二大", "盛產楓糖漿"]),
    ("維德角", "🇨🇻", "普拉亞", ["大西洋島國", "葡萄牙語系", "火山群島"]),
    ("中非共和國", "🇨🇫", "班基", ["非洲心臟", "熱帶雨林", "鑽石資源"]),
    ("查德", "🇹🇩", "恩將納", ["非洲死亡之心", "撒哈拉沙漠", "查德湖"]),
    ("智利", "🇨🇱", "聖地牙哥", ["國土形狀最狹長", "復活節島摩艾石像", "盛產紅酒"]),
    ("中國", "🇨🇳", "北京", ["萬里長城", "貓熊的故鄉", "人口最多的國家之一"]),
    ("哥倫比亞", "🇨🇴", "波哥大", ["盛產咖啡", "安地斯山脈", "祖母綠寶石"]),
    ("葛摩", "🇰🇲", "莫洛尼", ["香料群島", "印度洋", "依蘭依蘭"]),
    ("剛果共和國", "🇨🇬", "布拉柴維爾", ["剛果河", "赤道雨林", "前法國殖民地"]),
    ("剛果民主共和國", "🇨🇩", "金夏沙", ["非洲第二大國", "豐富礦產", "剛果河"]),
    ("哥斯大黎加", "🇨🇷", "聖荷西", ["沒有軍隊的國家", "豐富生態多樣性", "Pura Vida"]),
    ("克羅埃西亞", "🇭🇷", "薩格勒布", ["亞得里亞海", "冰與火之歌拍攝地", "紅白格紋"]),
    ("古巴", "🇨🇺", "哈瓦那", ["雪茄", "老爺車", "加勒比海島國"]),
    ("賽普勒斯", "🇨🇾", "尼科西亞", ["地中海島國", "愛神維納斯誕生地", "南北分裂"]),
    ("捷克", "🇨🇿", "布拉格", ["千塔之城", "波希米亞水晶", "皮爾森啤酒"]),
    ("丹麥", "🇩🇰", "哥本哈根", ["安徒生童話", "樂高積木發源地", "小美人魚雕像"]),
    ("吉布地", "🇩🇯", "吉布地市", ["紅海入口", "多國軍事基地", "阿薩勒湖(鹽湖)"]),
    ("多米尼克", "🇩🇲", "羅索", ["加勒比海自然之島", "沸騰湖", "鸚鵡國旗"]),
    ("多明尼加", "🇩🇴", "聖多明哥", ["加勒比海", "棒球強國", "海灘度假"]),
    ("東帝汶", "🇹🇱", "帝力", ["亞洲最年輕國家之一", "鱷魚傳說", "葡萄牙語系"]),
    ("厄瓜多", "🇪🇨", "基多", ["赤道之國", "加拉巴哥群島", "香蕉出口"]),
    ("埃及", "🇪🇬", "開羅", ["金字塔與人面獅身", "尼羅河", "古文明發源地"]),
    ("薩爾瓦多", "🇸🇻", "聖薩爾瓦多", ["火山之國", "中美洲", "衝浪勝地"]),
    ("赤道幾內亞", "🇬🇶", "馬拉博", ["非洲唯一西班牙語國", "石油", "位於赤道"]),
    ("厄利垂亞", "🇪🇷", "阿斯馬拉", ["紅海沿岸", "建築風格獨特", "非洲之角"]),
    ("愛沙尼亞", "🇪🇪", "塔林", ["波羅的海三小國", "數位化國家", "Skype發源地"]),
    ("史瓦帝尼", "🇸🇿", "墨巴本", ["非洲最後絕對君主制", "蘆葦節", "台灣邦交國"]),
    ("衣索比亞", "🇪🇹", "阿迪斯阿貝巴", ["咖啡發源地", "未被殖民過的非洲國家", "高原長跑"]),
    ("斐濟", "🇫🇯", "蘇瓦", ["南太平洋", "橄欖球強國", "換日線"]),
    ("芬蘭", "🇫🇮", "赫爾辛基", ["聖誕老人的故鄉", "千湖之國", "Nokia 的發源地"]),
    ("法國", "🇫🇷", "巴黎", ["艾菲爾鐵塔", "以美食與時尚聞名", "羅浮宮所在地"]),
    ("加彭", "🇬🇦", "利伯維爾", ["綠色金子(森林)", "石油", "大猩猩"]),
    ("甘比亞", "🇬🇲", "班珠爾", ["微笑海岸", "甘比亞河", "西非小國"]),
    ("喬治亞", "🇬🇪", "提比里斯", ["葡萄酒發源地", "高加索山", "卡查普里(起司麵包)"]),
    ("德國", "🇩🇪", "柏林", ["啤酒節", "以汽車工藝聞名 (BMW, Benz)", "曾經分裂為東德與西德"]),
    ("迦納", "🇬🇭", "阿克拉", ["黃金海岸", "可可", "阿散蒂王國"]),
    ("希臘", "🇬🇷", "雅典", ["奧運發源地", "藍白相間的建築", "西方文明搖籃"]),
    ("格瑞那達", "🇬🇩", "聖喬治", ["香料之島", "肉豆蔻", "加勒比海"]),
    ("瓜地馬拉", "🇬🇹", "瓜地馬拉市", ["馬雅文明中心", "提卡爾遺跡", "火山"]),
    ("幾內亞", "🇬🇳", "柯那克里", ["西非水塔", "鋁土礦", "尼日河發源"]),
    ("幾內亞比索", "🇬🇼", "比索", ["腰果", "群島", "前葡萄牙殖民地"]),
    ("蓋亞那", "🇬🇾", "喬治敦", ["南美英語國", "凱厄圖爾瀑布", "熱帶雨林"]),
    ("海地", "🇭🇹", "太子港", ["第一個黑人共和國", "巫毒教", "加勒比海"]),
    ("宏都拉斯", "🇭🇳", "德古西加巴", ["馬雅科潘遺跡", "香蕉共和國", "中美洲"]),
    ("匈牙利", "🇭🇺", "布達佩斯", ["多瑙河明珠", "溫泉浴場", "魔術方塊發明地"]),
    ("冰島", "🇮🇸", "雷克雅維克", ["冰火之國", "極光", "藍湖溫泉"]),
    ("印度", "🇮🇳", "新德里", ["泰姬瑪哈陵", "寶萊塢電影", "咖哩與香料"]),
    ("印尼", "🇮🇩", "雅加達", ["萬島之國", "峇里島", "人口最多的穆斯林國家"]),
    ("伊朗", "🇮🇷", "德黑蘭", ["波斯地毯", "古波斯帝國", "藏紅花"]),
    ("伊拉克", "🇮🇶", "巴格達", ["兩河流域", "巴比倫", "一千零一夜"]),
    ("愛爾蘭", "🇮🇪", "都柏林", ["踢踏舞", "吉尼斯黑啤酒", "翡翠島"]),
    ("以色列", "🇮🇱", "耶路撒冷", ["死海", "三大宗教聖地", "科技新創強國"]),
    ("義大利", "🇮🇹", "羅馬", ["比薩斜塔", "披薩與義大利麵發源地", "羅馬競技場"]),
    ("象牙海岸", "🇨🇮", "亞穆苏克羅", ["可可生產第一", "大象之國", "法語系"]),
    ("牙買加", "🇯🇲", "京斯敦", ["雷鬼音樂", "短跑飛人博爾特", "藍山咖啡"]),
    ("日本", "🇯🇵", "東京", ["擁有著名的富士山", "動漫與壽司文化", "被稱為「日出之國」"]),
    ("約旦", "🇯🇴", "安曼", ["佩特拉古城", "死海", "瓦迪拉姆沙漠"]),
    ("哈薩克", "🇰🇿", "阿斯塔納", ["中亞最大國", "蘋果發源地", "騎馬民族"]),
    ("肯亞", "🇰🇪", "奈洛比", ["動物大遷徙", "長跑選手", "馬賽馬拉"]),
    ("吉里巴斯", "🇰🇮", "塔拉瓦", ["跨越赤道與換日線", "珊瑚礁島國", "最早迎接日出"]),
    ("科威特", "🇰🇼", "科威特城", ["石油富國", "波斯灣", "水塔建築"]),
    ("吉爾吉斯", "🇰🇬", "比斯凱克", ["天山山脈", "伊塞克湖", "游牧文化"]),
    ("寮國", "🇱🇦", "永珍", ["東南亞唯一內陸國", "湄公河", "佛教"]),
    ("拉脫維亞", "🇱🇻", "里加", ["波羅的海三小國", "琥珀", "新藝術建築"]),
    ("黎巴嫩", "🇱🇧", "貝魯特", ["雪松", "中東小巴黎", "腓尼基文明"]),
    ("賴索托", "🇱🇸", "馬塞魯", ["天空王國", "被南非包圍", "高原地形"]),
    ("賴比瑞亞", "🇱🇷", "蒙羅維亞", ["美國釋放奴隸建立", "西非", "橡膠"]),
    ("利比亞", "🇱🇾", "的黎波里", ["撒哈拉沙漠", "古羅馬遺跡", "石油"]),
    ("列支敦斯登", "🇱🇮", "瓦都茲", ["雙重內陸國", "阿爾卑斯山", "郵票與假牙"]),
    ("立陶宛", "🇱🇹", "維爾紐斯", ["波羅的海三小國", "籃球強國", "十字架山"]),
    ("盧森堡", "🇱🇺", "盧森堡市", ["鋼鐵工業", "金融中心", "歐洲大公國"]),
    ("馬達加斯加", "🇲🇬", "安塔那那利佛", ["狐猴", "麵包樹", "世界第四大島"]),
    ("馬拉威", "🇲🇼", "里朗威", ["非洲溫暖之心", "馬拉威湖", "農業國"]),
    ("馬來西亞", "🇲🇾", "吉隆坡", ["雙子星塔", "多元種族文化", "盛產榴槤"]),
    ("馬爾地夫", "🇲🇻", "馬利", ["度假勝地", "珊瑚礁", "海拔最低國家"]),
    ("馬利", "🇲🇱", "巴馬科", ["廷巴克圖", "撒哈拉沙漠", "西非古帝國"]),
    ("馬爾他", "🇲🇹", "瓦萊塔", ["地中海心臟", "騎士團", "權力遊戲拍攝地"]),
    ("馬紹爾群島", "🇲🇭", "馬久羅", ["比基尼環礁", "太平洋", "核試驗歷史"]),
    ("茅利塔尼亞", "🇲🇷", "諾克少", ["撒哈拉之眼", "鐵礦", "沙漠國家"]),
    ("模里西斯", "🇲🇺", "路易士港", ["渡渡鳥故鄉", "七色土", "印度洋明珠"]),
    ("墨西哥", "🇲🇽", "墨西哥城", ["塔可餅 (Tacos)", "亡靈節", "馬雅文明遺跡"]),
    ("密克羅尼西亞", "🇫🇲", "帕利基爾", ["太平洋島國", "雅浦島石幣", "潛水勝地"]),
    ("摩爾多瓦", "🇲🇩", "基希訥烏", ["葡萄酒", "東歐", "修道院"]),
    ("摩納哥", "🇲🇨", "摩納哥", ["賭場", "F1賽車", "世界第二小國"]),
    ("蒙古", "🇲🇳", "烏蘭巴托", ["成吉思汗", "戈壁沙漠", "游牧民族"]),
    ("蒙特內哥羅", "🇲🇪", "波德戈里察", ["黑山", "亞得里亞海", "峽灣"]),
    ("摩洛哥", "🇲🇦", "拉巴特", ["卡薩布蘭卡", "撒哈拉沙漠", "阿甘油"]),
    ("莫三比克", "🇲🇿", "馬布多", ["印度洋海岸", "腰果", "葡萄牙語系"]),
    ("緬甸", "🇲🇲", "奈比多", ["萬塔之國", "翁山蘇姬", "玉石"]),
    ("納米比亞", "🇳🇦", "溫荷克", ["納米比沙漠", "紅沙漠", "骷髏海岸"]),
    ("諾魯", "🇳🇷", "亞倫", ["磷酸鹽", "世界最小島國", "沒有首都(行政中心)"]),
    ("尼泊爾", "🇳🇵", "加德滿都", ["聖母峰", "佛陀誕生地", "非矩形國旗"]),
    ("荷蘭", "🇳🇱", "阿姆斯特丹", ["風車與鬱金香", "運河城市", "自行車非常普及"]),
    ("紐西蘭", "🇳🇿", "威靈頓", ["魔戒拍攝地", "奇異果與奇異鳥", "毛利文化"]),
    ("尼加拉瓜", "🇳🇮", "馬拿瓜", ["火山與湖泊", "中美洲", "雪茄"]),
    ("尼日", "🇳🇪", "尼阿美", ["撒哈拉沙漠", "鈾礦", "恐龍化石"]),
    ("奈及利亞", "🇳🇬", "阿布賈", ["非洲人口最多", "諾萊塢電影", "石油"]),
    ("北韓", "🇰🇵", "平壤", ["封閉國家", "阿里郎表演", "38度線"]),
    ("北馬其頓", "🇲🇰", "史高比耶", ["亞歷山大大帝", "巴爾幹半島", "奧赫里德湖"]),
    ("挪威", "🇳🇴", "奧斯陸", ["峽灣地形", "維京人歷史", "盛產鮭魚"]),
    ("阿曼", "🇴🇲", "馬斯喀特", ["乳香", "阿拉伯古堡", "沙漠與海洋"]),
    ("巴基斯坦", "🇵🇰", "伊斯蘭瑪巴德", ["K2峰", "印度河文明", "板球"]),
    ("帛琉", "🇵🇼", "恩吉魯穆德", ["水母湖", "潛水天堂", "太平洋島國"]),
    ("巴勒斯坦", "🇵🇸", "拉姆安拉", ["聖地", "橄欖樹", "死海"]),
    ("巴拿馬", "🇵🇦", "巴拿馬城", ["巴拿馬運河", "連通兩大洋", "瑰夏咖啡"]),
    ("巴布亞紐幾內亞", "🇵🇬", "莫士比港", ["極樂鳥", "部落文化", "語言種類最多"]),
    ("巴拉圭", "🇵🇾", "亞松森", ["南美心臟", "瓜拉尼語", "伊泰普水電站"]),
    ("秘魯", "🇵🇪", "利馬", ["馬丘比丘", "羊駝", "印加帝國"]),
    ("菲律賓", "🇵🇭", "馬尼拉", ["由七千多個島嶼組成", "吉普尼車 (Jeepney)", "曾受西班牙統治"]),
    ("波蘭", "🇵🇱", "華沙", ["蕭邦的故鄉", "二戰歷史", "餃子 (Pierogi)"]),
    ("葡萄牙", "🇵🇹", "里斯本", ["蛋塔的發源地", "大航海時代的先驅", "C羅的故鄉"]),
    ("卡達", "🇶🇦", "杜哈", ["世界上最富裕國家之一", "2022世界盃", "天然氣"]),
    ("羅馬尼亞", "🇷🇴", "布加勒斯特", ["吸血鬼傳說", "喀爾巴阡山", "布蘭城堡"]),
    ("俄羅斯", "🇷🇺", "莫斯科", ["世界上國土面積最大", "紅場與克里姆林宮", "伏特加酒"]),
    ("盧安達", "🇷🇼", "基加利", ["千丘之國", "山地大猩猩", "非洲新加坡"]),
    ("聖克里斯多福及尼維斯", "🇰🇳", "巴斯特爾", ["加勒比海", "火山島", "糖業"]),
    ("聖露西亞", "🇱🇨", "卡斯特里", ["皮通山", "香蕉", "蜜月勝地"]),
    ("聖文森及格瑞那丁", "🇻🇨", "金斯敦", ["加勒比海", "神鬼奇航拍攝地", "火山"]),
    ("薩摩亞", "🇼🇸", "阿皮亞", ["波里尼西亞文化", "刺青", "橄欖球"]),
    ("聖馬力諾", "🇸🇲", "聖馬力諾", ["國中國", "現存最古老共和國", "郵票"]),
    ("聖多美普林西比", "🇸🇹", "聖多美", ["赤道島國", "可可", "非洲加拉巴哥"]),
    ("沙烏地阿拉伯", "🇸🇦", "利雅德", ["伊斯蘭教聖地麥加", "石油大國", "沙漠氣候"]),
    ("塞內加爾", "🇸🇳", "達卡", ["達卡拉力賽", "玫瑰湖", "西非之門"]),
    ("塞爾維亞", "🇷🇸", "貝爾格勒", ["網球名將喬科維奇", "多瑙河", "巴爾幹半島"]),
    ("塞席爾", "🇸🇨", "維多利亞", ["海椰子", "蜜月天堂", "花崗岩島嶼"]),
    ("獅子山", "🇸🇱", "弗里敦", ["血鑽石歷史", "黑猩猩", "西非"]),
    ("新加坡", "🇸🇬", "新加坡", ["魚尾獅公園", "花園城市", "東南亞金融中心"]),
    ("斯洛伐克", "🇸🇰", "布拉提斯拉瓦", ["城堡之國", "塔特拉山", "中歐汽車廠"]),
    ("斯洛維尼亞", "🇸🇮", "盧布爾雅那", ["布萊德湖", "鐘乳石洞", "阿爾卑斯山"]),
    ("索羅門群島", "🇸🇧", "荷尼阿拉", ["二戰瓜達康納爾島", "潛水", "美拉尼西亞"]),
    ("索馬利亞", "🇸🇴", "摩加迪休", ["非洲之角", "駱駝", "海盜歷史"]),
    ("南非", "🇿🇦", "普勒托利亞", ["彩虹國度", "盛產鑽石與黃金", "好望角"]),
    ("南韓", "🇰🇷", "首爾", ["泡菜與燒肉", "K-POP 流行文化", "三星電子的故鄉"]),
    ("南蘇丹", "🇸🇸", "朱巴", ["世界上最年輕國家", "尼羅河", "石油"]),
    ("西班牙", "🇪🇸", "馬德里", ["鬥牛活動", "佛朗明哥舞", "聖家堂"]),
    ("斯里蘭卡", "🇱🇰", "可倫坡", ["錫蘭紅茶", "印度洋淚珠", "大象孤兒院"]),
    ("蘇丹", "🇸🇩", "喀土穆", ["尼羅河匯流", "金字塔比埃及多", "沙漠"]),
    ("蘇利南", "🇸🇷", "帕拉馬里博", ["南美荷蘭語國", "熱帶雨林", "多元種族"]),
    ("瑞典", "🇸🇪", "斯德哥爾摩", ["IKEA 與 H&M", "諾貝爾獎頒發地", "北歐國家"]),
    ("瑞士", "🇨🇭", "伯恩", ["阿爾卑斯山", "精密鐘錶與軍刀", "永久中立國"]),
    ("敘利亞", "🇸🇾", "大馬士革", ["大馬士革玫瑰", "古文明", "歷史遺跡"]),
    ("塔吉克", "🇹🇯", "杜尚別", ["帕米爾高原", "中亞", "波斯語系"]),
    ("坦尚尼亞", "🇹🇿", "杜篤瑪", ["吉力馬札羅山", "塞倫蓋蒂", "桑吉巴島"]),
    ("泰國", "🇹🇭", "曼谷", ["佛教寺廟眾多", "泰式酸辣湯 (冬蔭功)", "大象是吉祥象徵"]),
    ("多哥", "🇹🇬", "洛美", ["西非", "巫毒市場", "狹長國土"]),
    ("東加", "🇹🇴", "努瓜婁發", ["友誼群島", "未被殖民的太平洋王國", "賞鯨"]),
    ("千里達及托巴哥", "🇹🇹", "西班牙港", ["鋼鼓音樂", "嘉年華", "瀝青湖"]),
    ("突尼西亞", "🇹🇳", "突尼斯", ["迦太基遺跡", "藍白小鎮", "星際大戰拍攝地"]),
    ("土耳其", "🇹🇷", "安卡拉", ["橫跨歐亞兩洲", "熱氣球勝地卡帕多奇亞", "烤肉 (Kebab)"]),
    ("土庫曼", "🇹🇲", "阿什哈巴特", ["地獄之門", "白色大理石城", "汗血寶馬"]),
    ("吐瓦魯", "🇹🇻", "富納富提", ["網域.tv", "受海平面上升威脅", "珊瑚礁"]),
    ("烏干達", "🇺🇬", "坎帕拉", ["非洲明珠", "維多利亞湖", "山地大猩猩"]),
    ("烏克蘭", "🇺🇦", "基輔", ["東歐糧倉", "向日葵", "車諾比核電廠"]),
    ("阿聯酋", "🇦🇪", "阿布達比", ["杜拜塔", "帆船飯店", "沙漠奇蹟"]),
    ("英國", "🇬🇧", "倫敦", ["大笨鐘與白金漢宮", "曾經是「日不落帝國」", "喜愛喝下午茶"]),
    ("美國", "🇺🇸", "華盛頓特區", ["好萊塢電影產業", "自由女神像", "由50個州組成"]),
    ("烏拉圭", "🇺🇾", "蒙特維多", ["第一屆世界盃主辦", "馬黛茶", "南美瑞士"]),
    ("烏茲別克", "🇺🇿", "塔什干", ["絲路古城", "撒馬爾罕", "雙重內陸國"]),
    ("萬那杜", "🇻🇺", "維拉港", ["高空彈跳發源地", "活火山", "水下郵局"]),
    ("梵蒂岡", "🇻🇦", "梵蒂岡城", ["世界最小國家", "教宗", "聖彼得大教堂"]),
    ("委內瑞拉", "🇻🇪", "卡拉卡斯", ["安赫爾瀑布(最高)", "石油儲量第一", "選美王國"]),
    ("越南", "🇻🇳", "河內", ["河粉 (Pho)", "下龍灣", "機車數量非常多"]),
    ("葉門", "🇾🇪", "沙那", ["摩天樓古城", "龍血樹", "阿拉伯半島"]),
    ("尚比亞", "🇿🇲", "路沙卡", ["維多利亞瀑布", "銅礦", "尚比西河"]),
    ("辛巴威", "🇿🇼", "哈拉雷", ["維多利亞瀑布", "石雕藝術", "大辛巴威遺跡"])
]

class FlagQuizApp:
    def __init__(self, root):
        self.root = root
        self.root.title("世界國旗猜謎大挑戰")
        self.root.geometry("600x750")
        self.root.configure(bg="#f0f0f0")

        # 遊戲設定
        self.total_rounds = 10  # 總題數
        self.time_limit = 15    # 每題秒數
        self.base_score = 10    # 每題基礎分
        self.hint_penalty = 2   # 提示扣分
        self.capital_bonus = 5  # 首都加分

        # 遊戲狀態
        self.current_round = 0
        self.score = 0
        self.high_score = self.load_high_score()
        self.current_question = None
        self.timer_id = None
        self.time_left = 0
        self.hints_used = 0
        self.used_countries = []

        # 初始化介面
        self.setup_start_screen()

    def load_high_score(self):
        try:
            if getattr(sys, 'frozen', False):
                base_path = os.path.dirname(sys.executable)
            else:
                base_path = os.path.dirname(os.path.abspath(__file__))
            path = os.path.join(base_path, "highscore.txt")
            if os.path.exists(path):
                with open(path, "r") as f:
                    return int(f.read().strip())
        except:
            pass
        return 0

    def save_high_score(self, score):
        try:
            if getattr(sys, 'frozen', False):
                base_path = os.path.dirname(sys.executable)
            else:
                base_path = os.path.dirname(os.path.abspath(__file__))
            path = os.path.join(base_path, "highscore.txt")
            with open(path, "w") as f:
                f.write(str(score))
        except:
            pass

    def setup_start_screen(self):
        self.clear_screen()
        
        tk.Label(self.root, text="🌍 世界國旗猜謎 🌍", font=("Arial", 32, "bold"), bg="#f0f0f0", fg="#333").pack(pady=50)
        
        tk.Label(self.root, text=f"🏆 最高分紀錄: {self.high_score}", font=("Arial", 16, "bold"), bg="#f0f0f0", fg="#FF5722").pack(pady=10)

        desc = (f"規則說明：\n\n"
                f"1. 共 {self.total_rounds} 題，每題 {self.time_limit} 秒。\n"
                f"2. 答對得 {self.base_score} 分。\n"
                f"3. 使用提示扣 {self.hint_penalty} 分。\n"
                f"4. 答對後輸入正確首都可額外加 {self.capital_bonus} 分！")
        
        tk.Label(self.root, text=desc, font=("Arial", 14), bg="#f0f0f0", justify="left").pack(pady=20)
        
        tk.Button(self.root, text="開始遊戲", font=("Arial", 16, "bold"), bg="#4CAF50", fg="white", 
                  command=self.start_game, width=15, height=2).pack(pady=30)

    def start_game(self):
        self.score = 0
        self.current_round = 0
        self.used_countries = []
        self.setup_game_ui()
        self.next_question()

    def setup_game_ui(self):
        self.clear_screen()
        
        # 頂部資訊欄
        info_frame = tk.Frame(self.root, bg="#f0f0f0")
        info_frame.pack(fill="x", padx=20, pady=10)
        
        self.lbl_round = tk.Label(info_frame, text="", font=("Arial", 12, "bold"), bg="#f0f0f0")
        self.lbl_round.pack(side="left")
        
        self.lbl_score = tk.Label(info_frame, text="分數: 0", font=("Arial", 12, "bold"), bg="#f0f0f0", fg="blue")
        self.lbl_score.pack(side="right")

        # 倒數計時條
        self.lbl_timer = tk.Label(self.root, text="", font=("Arial", 16, "bold"), bg="#f0f0f0", fg="red")
        self.lbl_timer.pack(pady=5)

        # 國旗顯示區 (字體放大以顯示 Emoji)
        self.lbl_flag = tk.Label(self.root, text="", font=("Segoe UI Emoji", 100), bg="#f0f0f0")
        self.lbl_flag.pack(pady=10)

        # 提示區
        self.lbl_hint = tk.Label(self.root, text="", font=("Arial", 12), bg="#f0f0f0", fg="#666", wraplength=500)
        self.lbl_hint.pack(pady=5)
        
        self.btn_hint = tk.Button(self.root, text="💡 查看提示 (-2分)", command=self.show_hint, bg="#FFC107")
        self.btn_hint.pack(pady=5)

        # 選項按鈕區
        self.options_frame = tk.Frame(self.root, bg="#f0f0f0")
        self.options_frame.pack(pady=20)
        
        self.option_buttons = []
        for i in range(4):
            btn = tk.Button(self.options_frame, text="", font=("Arial", 14), width=20, height=2,
                            command=lambda idx=i: self.check_answer(idx))
            btn.grid(row=i//2, column=i%2, padx=10, pady=10)
            self.option_buttons.append(btn)

        # 結果顯示區 (取代彈出視窗)
        self.lbl_result = tk.Label(self.root, text="", font=("Arial", 16, "bold"), bg="#f0f0f0")
        self.lbl_result.pack(pady=10)

    def next_question(self):
        if self.current_round >= self.total_rounds:
            self.game_over()
            return

        self.current_round += 1
        self.hints_used = 0
        self.lbl_hint.config(text="")
        self.btn_hint.config(state="normal", text=f"💡 查看提示 (-{self.hint_penalty}分)")
        self.lbl_result.config(text="")
        self.lbl_round.config(text=f"第 {self.current_round}/{self.total_rounds} 題")
        self.lbl_score.config(text=f"分數: {self.score}")

        # 隨機選題 (不重複)
        available = [c for c in COUNTRY_DATA if c[0] not in self.used_countries]
        if not available:
            available = COUNTRY_DATA # 如果題目不夠，重置
            self.used_countries = []
        
        self.current_question = random.choice(available)
        self.used_countries.append(self.current_question[0])
        
        # 設定正確答案與干擾項
        correct_country = self.current_question[0]
        distractors = random.sample([c[0] for c in COUNTRY_DATA if c[0] != correct_country], 3)
        self.options = distractors + [correct_country]
        random.shuffle(self.options)

        # 更新 UI
        # 嘗試載入圖片 (請在程式同層目錄建立 flags 資料夾，放入 "國家名.png")
        # 判斷是腳本執行還是打包後的 EXE，取得正確的基礎路徑
        if getattr(sys, 'frozen', False):
            base_path = os.path.dirname(sys.executable)
        else:
            base_path = os.path.dirname(os.path.abspath(__file__))
        
        # 搜尋圖片 (優先找 png, 其次 jpg)
        img_path = None
        for ext in [".png", ".jpg", ".jpeg"]:
            p = os.path.join(base_path, "flags", f"{self.current_question[0]}{ext}")
            if os.path.exists(p):
                img_path = p
                break

        if img_path:
            try:
                if img_path.endswith(".png") and not Image:
                    # 沒有安裝 Pillow，只能讀取 PNG
                    self.current_img = tk.PhotoImage(file=img_path)
                    if self.current_img.width() > 300:
                        factor = self.current_img.width() // 300
                        self.current_img = self.current_img.subsample(factor)
                elif Image:
                    # 有安裝 Pillow，可讀取 JPG/PNG 並有更好的縮放品質
                    pil_img = Image.open(img_path)
                    if pil_img.width > 300:
                        ratio = 300 / float(pil_img.width)
                        new_height = int(float(pil_img.height) * ratio)
                        pil_img = pil_img.resize((300, new_height), getattr(Image, 'Resampling', Image).LANCZOS)
                    self.current_img = ImageTk.PhotoImage(pil_img)
                else:
                    # 只有 JPG 但沒安裝 Pillow
                    raise ImportError("JPG requires Pillow library")

                self.lbl_flag.config(image=self.current_img, text="")
            except Exception:
                self.lbl_flag.config(image="", text=self.current_question[1])
        else:
            self.lbl_flag.config(image="", text=self.current_question[1])

        for i, btn in enumerate(self.option_buttons):
            btn.config(text=self.options[i], state="normal", bg="#e1e1e1")

        # 啟動計時器
        self.time_left = self.time_limit
        self.update_timer()

    def update_timer(self):
        if self.time_left > 0:
            self.lbl_timer.config(text=f"⏰ 剩餘時間: {self.time_left} 秒")
            self.time_left -= 1
            self.timer_id = self.root.after(1000, self.update_timer)
        else:
            self.lbl_timer.config(text="⏰ 時間到！")
            self.handle_timeout()

    def show_hint(self):
        if self.hints_used < len(self.current_question[3]):
            hint_text = self.current_question[3][self.hints_used]
            current_text = self.lbl_hint.cget("text")
            new_text = f"{current_text}\n提示 {self.hints_used + 1}: {hint_text}" if current_text else f"提示 {self.hints_used + 1}: {hint_text}"
            self.lbl_hint.config(text=new_text)
            self.hints_used += 1
            
            # 更新按鈕文字
            if self.hints_used >= len(self.current_question[3]):
                self.btn_hint.config(text="沒有更多提示了", state="disabled")
        else:
            self.btn_hint.config(state="disabled")

    def check_answer(self, idx):
        # 停止計時
        if self.timer_id:
            self.root.after_cancel(self.timer_id)

        selected = self.options[idx]
        correct = self.current_question[0]

        # 鎖定按鈕
        for btn in self.option_buttons:
            btn.config(state="disabled")

        if selected == correct:
            # 計算得分
            points = self.base_score - (self.hints_used * self.hint_penalty)
            points = max(0, points) # 分數不低於 0
            self.score += points
            self.option_buttons[idx].config(bg="#90EE90") # 綠色
            self.lbl_result.config(text=f"✅ 答對了！獲得 {points} 分。", fg="green")
            
            # 首都加分題
            self.root.after(1000, self.ask_capital_bonus)
        else:
            self.option_buttons[idx].config(bg="#FFB6C1") # 紅色
            # 顯示正確答案
            correct_idx = self.options.index(correct)
            self.option_buttons[correct_idx].config(bg="#90EE90")
            self.lbl_result.config(text=f"❌ 答錯了... 正確答案是: {correct}", fg="red")
            self.root.after(2000, self.next_question)

    def handle_timeout(self):
        # 鎖定按鈕
        for btn in self.option_buttons:
            btn.config(state="disabled")
        
        correct = self.current_question[0]
        correct_idx = self.options.index(correct)
        self.option_buttons[correct_idx].config(bg="#90EE90")
        
        self.lbl_result.config(text=f"⏰ 時間到！正確答案是: {correct}", fg="red")
        self.root.after(2000, self.next_question)

    def ask_capital_bonus(self):
        correct_capital = self.current_question[2]
        user_capital = simpledialog.askstring("加分題", f"請問 {self.current_question[0]} 的首都是哪裡？")
        
        if user_capital:
            # 簡單的模糊比對 (去除空白)
            if user_capital.strip() == correct_capital:
                self.score += self.capital_bonus
                self.lbl_score.config(text=f"分數: {self.score}")
                self.lbl_result.config(text=f"🎉 首都答對！額外獲得 {self.capital_bonus} 分！", fg="green")
            else:
                self.lbl_result.config(text=f"😅 首都答錯囉。正確首都是: {correct_capital}", fg="#d9534f")
        else:
            # 使用者取消或沒輸入
            self.lbl_result.config(text="未回答首都加分題。", fg="gray")

        self.root.after(2000, self.next_question)

    def game_over(self):
        self.clear_screen()
        
        # 計算滿分 (假設每題都答對且答對首都，且不看提示)
        max_possible = self.total_rounds * (self.base_score + self.capital_bonus)
        percentage = (self.score / max_possible) * 100

        is_new_record = False
        if self.score > self.high_score:
            self.high_score = self.score
            self.save_high_score(self.high_score)
            is_new_record = True

        # 評級系統
        rank = "地理小白"
        color = "gray"
        if percentage >= 90:
            rank = "地理 Master (大師)"
            color = "gold"
        elif percentage >= 75:
            rank = "環遊世界達人"
            color = "orange"
        elif percentage >= 60:
            rank = "地理資優生"
            color = "blue"
        elif percentage >= 40:
            rank = "努力中的旅人"
            color = "green"

        tk.Label(self.root, text="遊戲結束", font=("Arial", 32, "bold"), bg="#f0f0f0").pack(pady=30)
        tk.Label(self.root, text=f"最終得分: {self.score} / {max_possible}", font=("Arial", 24), bg="#f0f0f0").pack(pady=10)
        
        if is_new_record:
            tk.Label(self.root, text="🎉 恭喜！刷新最高分紀錄！ 🎉", font=("Arial", 20, "bold"), bg="#f0f0f0", fg="red").pack(pady=5)
        else:
            tk.Label(self.root, text=f"最高分紀錄: {self.high_score}", font=("Arial", 16), bg="#f0f0f0", fg="#666").pack(pady=5)

        rank_lbl = tk.Label(self.root, text=f"稱號: {rank}", font=("Arial", 28, "bold"), bg="#f0f0f0", fg=color)
        rank_lbl.pack(pady=20)

        btn_frame = tk.Frame(self.root, bg="#f0f0f0")
        btn_frame.pack(pady=40)

        tk.Button(btn_frame, text="再玩一次", font=("Arial", 14), command=self.start_game, width=12, bg="#4CAF50", fg="white").pack(side="left", padx=20)
        tk.Button(btn_frame, text="離開遊戲", font=("Arial", 14), command=self.root.quit, width=12, bg="#f44336", fg="white").pack(side="right", padx=20)

    def clear_screen(self):
        for widget in self.root.winfo_children():
            widget.destroy()

if __name__ == "__main__":
    root = tk.Tk()
    app = FlagQuizApp(root)
    root.mainloop()